# Kernel-Layer Literature Research: Index

Deep literature research on the topics a CAD geometry kernel needs beyond curve/surface mathematics: topology, operations, system engineering, and adjacent representations. Companion to the NURBS collection in `docs/research/nurbs/` and the original six tracks in `docs/research/`. Roughly 1306 cited sources across fifty-five dossiers plus two capability maps (Parasolid, XNURBS), in the same full-findings format (per-source citation, explicit mechanism, limitations, kernel relevance). Files 39 (coincident/tangent face booleans), 40 (blend/fillet surface generation), 41 (blend overflow and radius-vs-feature failures), 44 (concave-fillet topology surgery), 45 (genus-1 solid-of-revolution Euler sequence), 46 (face-orientation sense convention), 47 (boolean result assembly), 48-52 (skinning/lofting, sweep with RMF, shell/offset/thicken, sheet bodies, persistent naming), and 53-57 (vertex/setback blends, partial-span blends and runouts, blend networks and mitring, blend overflow handling, non-manifold general booleans, the last five tractable parity engines) were direct, on-critical-path requests from the implementation team. Files 42-43 are a non-English-language pass (Russian C3D/Golovanov, Chinese CAGD and domestic kernels) checking whether the English-only corpus left gaps. Files 01-10 are the first round; files 11-20 cover topics surfaced by that round; files 21-24 are the third round closing literature gaps; file 25 is an authoritative Parasolid capability audit (144 items) and files 26-28 are the fourth round closing the operation gaps that audit confirmed; files 29-30 are the dirty-geometry round (Parasolid's strongest, most-differentiating capability); file 31 is an XNURBS capability audit and files 32-33 are the surfacing round it drove; files 34-37 are the engineering round (testing/corpus, verification, performance, numerical substrate) that shifts from what the kernel does to how it is made fast and proven correct; file 38 is the STEP AP242 import dossier, the one shippable-path literature gap the completeness audit (docs/research/03-completeness-audit.md) flagged.

## Files

| File | Scope | Sources |
|---|---|---|
| [01-topology-euler-operators.md](01-topology-euler-operators.md) | B-rep data structures (winged-edge, half-edge, quad-edge), Euler operators and Mantyla's completeness result, Euler-Poincare invariants, Requicha's r-sets and regularized booleans, boundary evaluation, STEP topology, kernel hierarchy comparisons | 27 |
| [02-nonmanifold-cellular.md](02-nonmanifold-cellular.md) | Weiler radial edge, partial entity structure, SGC, NOODLES, cellular topology, non-manifold Euler operators, G-maps and combinatorial maps, LAR chain complexes, how Parasolid/ACIS/OCCT each handle non-manifoldness | 24 |
| [03-local-operations-direct-editing.md](03-local-operations-direct-editing.md) | Tweak, delete-face with wound healing, blend recognition and removal, defeaturing, direct modeling (Synchronous Technology, variational direct modeling), surface extension, local-op robustness, kernel API anatomies | 25 |
| [04-constraint-solving-2d-profiles.md](04-constraint-solving-2d-profiles.md) | Geometric constraint solving (Owen, Fudos-Hoffmann, DOF analysis, witness configuration), 3D assembly constraints, solver landscape (DCM, PlanGCS, SolveSpace); 2D curve arrangements, region extraction, planar booleans, curve offsetting, 2D robustness | ~26 |
| [05-tessellation-faceting.md](05-tessellation-faceting.md) | Trimmed-surface triangulation, CDT and Delaunay refinement, sag/normal/edge-length criteria, metric-driven anisotropic meshing, watertight faceting across shared edges, industrial faceters (Parasolid, ACIS, OCCT BRepMesh), tolerant remeshing | 26 |
| [06-interrogation-hlr.md](06-interrogation-hlr.md) | Fundamental forms and curvature fields, umbilics, isophotes/reflection/highlight lines, silhouettes and draft analysis, mold separability, geodesics (exact and discrete), flattening, hidden line removal and quantitative invisibility | 24 |
| [07-persistent-naming-sessions.md](07-persistent-naming-sessions.md) | The topological naming problem: Kripac, Capoyleas-Chen-Hoffmann, Marcheix-Pierra, Farjana-Han review, FreeCAD/realthunder fix, OnShape query model, Cascaval lineage DSL; Parasolid partitions/pmarks/deltas, ACIS bulletin boards, undo, journaling, transactionality, concurrency | 24 |
| [08-kernel-engineering.md](08-kernel-engineering.md) | Spatial acceleration (BVH variants, box filters, incremental refit), testing methodology (ABC/Thingi10K corpora, differential and metamorphic testing, fuzzing, FP reproducibility), API design study (PK, ACIS, OCCT, Rust handle arenas) | 25 |
| [09-mesh-implicit-hybrid.md](09-mesh-implicit-hybrid.md) | Exact mesh booleans (CGAL, libigl, EMBER, Cherchi/Attene indirect predicates), tolerant repair (TetWild family), winding numbers, convergent modeling; implicit/SDF modeling (R-functions, ADFs, OpenVDB, dual contouring), lattices/TPMS, conversion boundaries | 27 |
| [10-minkowski-medial-midsurface.md](10-minkowski-medial-midsurface.md) | Minkowski sums (exact and convolution), morphology view of filleting, swept volume envelope theory (grazing condition, SDE, continuation), 5-axis envelopes; medial axis theory and computation, pruning, power crust, Q-MAT, midsurface extraction, thickness analysis | 28 |
| [11-curved-exact-predicates.md](11-curved-exact-predicates.md) | Exact geometric computation for curved geometry: separation bounds, filtered/lazy arithmetic, circular and conic kernels (EXACUS, CGAL), quadric intersection parameterization (QI), algebraic kernels, spline certification, symbolic perturbation, why commercial kernels chose tolerances | 42 |
| [12-3d-constraints-direct-modeling.md](12-3d-constraints-direct-modeling.md) | 3D constraint solving on live B-reps: Hoffmann-Vermeer, witness configuration diagnosis, minimal-motion solving, D-Cubed 3D DCM and Live Rules, variational direct modeling (Zou-Feng), PushPull++, relation inference and beautification (Langbein), drag continuity | 24 |
| [13-import-healing-extension.md](13-import-healing-extension.md) | Import healing pipelines: gap taxonomy, stitching (Barequet-Sharir), ACIS HEAL and CADfix phase structure, pcurve regeneration, analytic simplification, PDQ standards, heal-vs-tolerate policy; B-spline surface extension methods and extrapolation stability | 22 |
| [14-determinism-serialization.md](14-determinism-serialization.md) | FP reproducibility (FMA hazards, correctly-rounded libm, Rust specifics), reproducible parallel reduction, FLiT/Verificarlo, non-FP determinism sources; XT/SAT/BRep format anatomy, schema evolution, hostile-input deserialization, exact double round-tripping | ~32 |
| [15-feature-recognition.md](15-feature-recognition.md) | Classical recognition (AAG, hint-based OOFF/IF2, volumetric decomposition), feature families (holes, pockets, blends, patterns), the ML wave (FeatureNet, UV-Net, BRepNet, AAGNet, BRepFormer), datasets and their synthetic bias, neuro-symbolic propose-then-verify | 31 |
| [16-assembly-layer.md](16-assembly-layer.md) | Definition/occurrence DAGs, STEP NAUO and OCCT XCAF, JT and lightweight formats, massive-model rendering (HLOD, GigaWalk), assembly-scale clash, shrinkwrap, OnShape collaboration model, large-coordinate precision management | ~20 |
| [17-gdt-pmi-tolerances.md](17-gdt-pmi-tolerances.md) | ASME Y14.5 / ISO GPS semantics, Requicha's offset-zone tolerance theory, T-maps, stack-up analysis (SDT, Jacobian-torsor), datum establishment as constrained fitting, semantic PMI in AP242/JT/QIF, NIST fidelity studies, inspection connection | ~27 |
| [18-patent-ip-landscape.md](18-patent-ip-landscape.md) | Defensive patent survey: expired foundations (T-splines expired 2024), active risk families (Synchronous Technology to ~2034, convergent modeling ~2035+, U-splines ~2037, lattice/implicit), OSS precedents (OpenSubdiv grant), license implications, per-subsystem risk matrix | ~27 |
| [19-parametric-regeneration.md](19-parametric-regeneration.md) | History-based regeneration: feature dependency DAGs, minimal recompute, OnShape microversions, parametric fragility literature (RMS, horizontal modeling), parallel regeneration (negative finding: literature is thin), code-CAD and dataflow alternatives, UDFs, CAE association | 23 |
| [20-sheet-metal.md](20-sheet-metal.md) | Sheet metal as kernel ops: bend mathematics (K-factor, DIN 6935), anchored bend graph with cycles, exact cylindrical unwrap, flat/folded topology invariance, formed-feature handling, recognition from dumb solids, DFM rules, system implementations | 23 |
| [21-kinematics-motion.md](21-kinematics-motion.md) | Mate-to-joint semantics, Kramer TLA, screw theory and se(3) mobility (Gruebler failure cases), kinematic loop solving, assembly drag, multibody dynamics boundary (Featherstone, Drake/MuJoCo contracts), CCD along motion, joint-axis extraction from B-rep | 24 |
| [22-model-diff-comparison.md](22-model-diff-comparison.md) | B-rep equality oracles, CAx-IF/LOTAR validation properties, model differencing for version control, shape retrieval (D2, spherical harmonics, Reeb graphs, AAG matching, learned encoders), registration (ICP, Go-ICP), assembly diff, the comparison cascade | 22 |
| [23-metrology-fitting.md](23-metrology-fitting.md) | Fitting objective taxonomy (L2, min-zone, MIC/MCC, constrained-LS) mapped to ISO/ASME callouts, Shakarji NIST reference algorithms with Jacobians, exact min-zone certificates, RANSAC/robust modes, NIST ATS and PTB TraCIM validation, uncertainty, constrained simultaneous fitting | ~21 |
| [24-canonical-geometry-recovery.md](24-canonical-geometry-recovery.md) | Recognizing analytics hidden in splines: exact structure tests (Piegl-Tiller), curvature classification (Benko-Varady), linear line complex axis recovery (Pottmann), certification by max-deviation bound, topology-coordinated substitution (OCCT canonical recognition), constraint-aware re-fit | 22 |
| [25-parasolid-capability-map.md](25-parasolid-capability-map.md) | Authoritative inventory of Parasolid PK functionality grouped by area (creation, booleans, local ops, blending, sweeping, sheets, convergent/mesh, tessellation, interrogation, tolerant modeling, foreign geometry, attributes, sessions, repair) with a 144-item flat capability checklist; used as the coverage audit that drove the fourth round | reference |
| [26-transfinite-nsided-surfacing.md](26-transfinite-nsided-surfacing.md) | Coons and Gordon surfaces, Gregory twist-compatible patches, the n-sided patch families (Charrot-Gregory, Loop-DeRose S-patches, Krasauskas toric, GBC), ribbon-based transfinite surfacing (Varady-Salvi-Rockwood GB patch), curve-network/Class-A G2 surfacing, the vertex-enclosure parity constraint, central-split vs single-patch, exact NURBS conversion | 34 |
| [27-free-form-deformation.md](27-free-form-deformation.md) | Lattice FFD (Sederberg-Parry, EFFD, NURBS FFD), Barr global bend/twist/taper and the normal-transformation rule, direct manipulation, cage coordinates (mean-value, harmonic, Green), axial/wire deformation, ARAP/Laplacian/Poisson variational editing, emboss/wrap as composed operators, the deform-control-net-hold-domain B-rep contract, Gain-Dodgson injectivity | 22 |
| [28-fillet-chamfer-engine.md](28-fillet-chamfer-engine.md) | The complete edge-finishing engine: chamfer family (equal/two-offset/offset-angle/apex-range), unified spine + spring-curve + polymorphic-section machinery, blend overflow and runout, hold-line blends (the engine inverted), range/setback, variable radius, face-face blends, networks and mitring, recognition and unblend, G2 sections, the commercial-failure-mode checklist | 21 |
| [29-dirty-geometry-graceful-degradation.md](29-dirty-geometry-graceful-degradation.md) | The "bad input still gets an answer" doctrine: best-effort vs all-or-nothing, fuzzy/tolerant operation modes (OCCT fuzzy value, ACIS/Parasolid tolerant modeling), partial results with fault tags, robustness cascades and algorithm portfolios, repair-in-the-loop, orientation/normal auto-correction, confidence reporting, mesh/SDF fallback as the floor, why robustness is an organizational (corpus) asset | 30 |
| [30-imperfect-brep-defect-handling.md](30-imperfect-brep-defect-handling.md) | Per-defect techniques: the 13-class defect taxonomy, virtual topology (composite faces, tolerate-by-hiding), self-intersection detection/resolution, sliver/short-edge remediation, degenerate-surface handling, snapping/zippering/merging under tolerance, orientation repair, mesh-based rescue (MeshFix, fTetWild, generalized winding numbers), re-validation after repair | 24 |
| [31-xnurbs-capability-map.md](31-xnurbs-capability-map.md) | Authoritative catalog of XNURBS (commercial energy-minimization surfacing): single global solve from arbitrary mixed constraints (boundary/internal curves, points, per-edge G0-G3 to neighbors), messy-input tolerance, hole/patch fill, quality/fairness, output and integrations, with verified/claimed/unconfirmed labels and a 60-item checklist; drove the surfacing round | reference |
| [32-variational-surface-design.md](32-variational-surface-design.md) | Energy-minimization surfacing (the XNURBS core): membrane/thin-plate/total-curvature/Willmore functionals stated explicitly, the deformable-model lineage (Terzopoulos, Celniker-Gossard, Welch-Witkin, Moreton-Sequin, Greiner, Botsch-Kobbelt), discretization to a control-point quadratic P^T K P, the unified (N^T W N + lambda K) fitting/fairing/variational engine, the wavy-interior failure mode, linear vs nonlinear (Class-A) modes | 18 |
| [33-constrained-surfacing-continuity.md](33-constrained-surfacing-continuity.md) | The constraint/continuity machinery: G0-G3 definitions, inter-patch continuity as conditions on control-point rows (DeRose, Liu-Hoschek), matching a fixed neighbor as linear pinned-row constraints (G1 pins 1 row, G2 pins 2, G3 pins 3), the KKT-solved constrained-least-squares master template, point/curve constraints, gap-tolerant soft constraints with slack, conflict/feasibility, corner compatibility, refine-until-feasible | 22 |
| [34-test-corpus-adversarial-engineering.md](34-test-corpus-adversarial-engineering.md) | Building the corpus as a product: adversarial/degenerate geometry generation (coincidence-first), grammar/construction-sequence generators, property-based design and degenerate-witness shrinking, coverage-guided fuzzing, public datasets (ABC, Thingi10K, Fusion 360 Gallery) and their clean-bias limits, failure triage to golden cases, metamorphic input pairs, the fTetWild pass-rate metric | 17 |
| [35-verification-validated-numerics-oracles.md](35-verification-validated-numerics-oracles.md) | Deciding an output is correct: the oracle problem, the metamorphic-relation catalog (boolean laws, round-trips, transform/knot-refinement invariance, mass-property inclusion-exclusion), differential testing and voting oracles, the validator as continuous oracle, validated/interval numerics (Krawczyk, CAPD), exact-oracle-for-easy-cases, formal-verification reach, reproducibility and instability detection, epsilon-solidity certification | 26 |
| [36-performance-parallelism-memory.md](36-performance-parallelism-memory.md) | Making it fast without breaking correctness: cache-coherent SoA layout, SIMD/Bezier-extraction evaluation, the safe-parallelism map (data-parallel read phases over copy-on-write snapshots, serial graph surgery), determinism under parallelism (binned reduction, deterministic merge), the GPU display-only verdict, lazy/incremental caching, out-of-core, allocation, benchmarking | 24 |
| [37-numerical-substrate.md](37-numerical-substrate.md) | The math-layer infrastructure: robust tiny dense solves, sparse direct (CHOLMOD/faer) vs iterative for the surfacing/KKT systems, QR-vs-normal-equations and the fairness term as Tikhonov regularizer, eigen/SVD for curvature/roots/inertia, autodiff vs analytic hodographs, interval/affine arithmetic with portable ULP-widening, globalized Newton/LM, smoothness-aware quadrature, conditioning discipline | 26 |
| [38-step-ap242-import.md](38-step-ap242-import.md) | Implementation-grade STEP AP242 import: Part 21 structure, the AP203/214/242 lineage, the Part 42 entity model, the centerpiece entity-to-Keel mapping table (topology, surfaces, curves), NURBS conversion to homogeneous 4D, units and the file's own uncertainty tolerance, pcurve reconstruction policy, the orientation algebra and seams/poles, validation-property round-trip, heal-on-import for dirty STEP, parser reuse-vs-build (build a focused pure-Rust Part 21 parser, borrow ruststep's pattern, OCC as conformance reference) | 20 |
| [39-coincident-tangent-face-booleans.md](39-coincident-tangent-face-booleans.md) | The on-on classification problem for curved B-rep booleans (M7b critical path): overlap-region imprint onto both faces, the two-sided winding-number neighborhood test that resolves the on-boundary ambiguity, the directly-encodable keep/drop/orient tables for union/intersection/difference by same- vs opposite-sense (Laidlaw-Trumbore-Hughes, Requicha-Voelcker, Mantyla nbr), tangent-face detection and handling, production gluing (OCCT BOPAlgo GlueShift/GlueFull, Parasolid matched regions, ACIS), the preserve-coincidence-not-perturb verdict against Simulation of Simplicity, and the analytic-then-NURBS coincidence-detection ladder | 24 |
| [40-blend-fillet-surface-generation.md](40-blend-fillet-surface-generation.md) | Implementation-grade fillet surface generation: the rolling-ball blend as a canal surface along a spine, the spine computed as the SSI of the two offset support surfaces, the exact-analytic catalog (line spine to cylinder, circle to torus, natural-quadric pairs to Dupin cyclide emitted as exact rational biquadratic NURBS, else NURBS canal), setback vertex blends via the transfinite n-sided patch, trim-and-stitch as a local imprint, variable-radius and chordal/asymmetric, G1 certification and radius-too-large rejection, and the declarative blend-recipe pipeline (OCCT ChFi3d, ACIS ABL, Parasolid) | 19 |
| [41-blend-overflow-radius-feature-failures.md](41-blend-overflow-radius-feature-failures.md) | Blend overflow and radius-vs-local-feature failure as a first-class topic: the medial-axis r_max(t) = local-feature-size predictor as a pre-generation feasibility pass, the 11-mode failure catalog (F1-F11: self-intersection, off-face-end, roll-on, cross-feature, cliff, notch, thin-wall pinch, swallow, blend-blend, vertex, over-large radius) each with a codeable detection predicate and ranked recovery, the best-effort recovery ladder (predict, roll-on, radius-limit, setback/cap/notch, partial+report, last-resort reject), and the recovery-oriented (Parasolid/ACIS) vs rejection-oriented (OCCT) vs never-fail-but-inexact (nTop implicit) pipeline contrast. Includes a dated primary-source verification of the Parasolid overflow vocabulary against the v12 docs | 19 |
| [42-russian-c3d-literature.md](42-russian-c3d-literature.md) | Russian-language pass (Golovanov's Geometric Modeling, the C3D Labs engineering blogs, ASCON/KOMPAS, the Soviet spline and descriptive-geometry tradition): honest verdict that the gap is small but the C3D tolerant-topology model is the single best find, tolerance bound to the topological entity (tolerant vertex = sphere of radius t, tolerant edge = tube of radius t), carried geometry stays exact; plus per-operation boolean precision as an API argument and connectivity repair by tolerant-vertex insertion | 12 |
| [43-chinese-cagd-literature.md](43-chinese-cagd-literature.md) | Chinese-language pass (JCAD&CG and Science China journals, the post-2020 domestic-kernel push): verdict that the journals hide no robustness silver bullet and corroborate the geometry-topology-consistency diagnosis, but the domestic-kernel engineering is genuinely new, AMCAX/Jiushao (USTC) is the only from-scratch kernel and chose a T-spline-first architecture with NURBS B-rep layered on (an inversion worth noting), plus a 2024 order-2-on-tangency derivative-free curve-intersection iteration and the PIA/LSPIA fitting lineage | 25 |
| [44-concave-fillet-topology-surgery.md](44-concave-fillet-topology-surgery.md) | The low-level Euler-operator surgery for a concave (reentrant, material-addition) edge fillet in a PES B-rep, where the cap face must grow and its reentrant corner becomes interior (the convex split-off-and-dissolve is the wrong operation): the grow-to-arc sequence as the make-dominant dual of the convex kill-dominant dissolve with full V/E/F bookkeeping and Euler-Poincare preservation, the unified pipeline forking only at the cap on a single convexity sign sigma = sign((n1 x n2).t), the in-place-loop-regrow vs bead-construct-and-sew decision (sew the tangent spring seam per file 39 as a tolerant edge per file 42), and how Parasolid/ACIS/OCCT classify convexity and extend rather than trim at the concave extremity | 18 |
| [45-genus1-solid-of-revolution-euler-sequence.md](45-genus1-solid-of-revolution-euler-sequence.md) | The GWB Euler-operator sequence for a genus-1 solid of revolution (tube/annular solid) from a closed off-axis meridian polygon of n segments, generalizing the single-face torus recipe to n faces: mvfs seed, per-segment mev+mef, a final cycle-closing mef back to the seed vertex, then one kfmrh that punches the handle (genus 0 to 1) and nowhere else, worked end-to-end for the hollow cylinder (V=4,E=8,F=4, V-E+F-R = 2(S-G) = 0); the holed-cap verdict (post-hoc kemr-the-seam, no genuine mekr-during alternative), per-band seam orthogonal to the handle punch, production kernels getting genus-1 emergently from a closed periodic meridian, and the pre-punch degeneracy guard (skip kfmrh if any pole or inner radius to zero) | 14 |
| [46-face-orientation-sense-convention.md](46-face-orientation-sense-convention.md) | The single canonical face-orientation invariant (n_out = sense x natural; material opposite n_out; outer loop CCW about n_out) and the fix for two divergent producers (Euler faces fin-based, boolean faces natural-normal-based) that split the sense/region convention and break analytic mass_properties on reversed-sense faces: production kernels (OCCT/ACIS/Parasolid) are sense-primary, but the recommendation for Keel is fin/material-primary with sense as a derived cache (natural normals flip under reparameterization and vanish at poles/seams), a single canonicalize_face_orientation writer plus a single face.outward_normal() consumer, a debug validator invariant, and confirmation that one shared outward-normal helper collapses the genus-1 mass_properties bug and the tilted-cut stitch-sense bug into one fix | 12 |
| [47-boolean-result-assembly.md](47-boolean-result-assembly.md) | Robust assembly of the boolean result B-rep without dropping kept fragments (the build_result_solid thin/oblique face-drop): the drop-prevention invariant (every kept coedge has exactly one radial partner or a complete radial cycle before the shell closes; an unmatched coedge is a hard error, never a silent drop), identity-preserving assembly (carry the imprint/seam EdgeId on every fragment coedge and pair by identity, never by coordinate re-weld), the soup-vs-Euler verdict (retire build_result_solid, generalize stitch_by_import to the planar path), the feature-size-relative weld tolerance (k*min edge length, or better no coordinate weld at all), what transfers from mesh-arrangement booleans (completeness-then-labeling, Sugihara-Iri topology-decided-combinatorially), and why this fixes the asymmetric chamfer | 19 |
| [48-nurbs-skinning-lofting.md](48-nurbs-skinning-lofting.md) | Smooth NURBS skinning/lofting (the gap dossier 26 deferred): degree-elevate to common p, Park-Lee common-knot determination plus constrained-energy refit instead of the exploding union merge, feature-correspondence reparameterization, averaged-chord v-knots, the per-column banded v-interpolation solve; G1/G2 ends and guide rails as pinned v-control-rows feeding the file-33 constrained-LS (rails make it a Gordon surface); differing-section refit; ruled as the q=1 case; periodic-section seam alignment; Lyche-Morken post-loft knot removal; validated on a 3-section circle-to-square tangent-ends skin | 17 |
| [49-sweep-rmf.md](49-sweep-rmf.md) | General sweep with rotation-minimizing frames: double-reflection RMF transport (Wang-Juttler-Zheng-Liu, O(h^4), exact on arcs) over Frenet (which flips at inflections and is undefined on straight legs); procedural spine+profile+frame baked to NURBS by tolerance-controlled skinning with a chord-height sampling rule; self-intersection when profile radius exceeds path curvature radius (r*kappa >= 1, the same r_max ceiling as files 10/41), decline for surfaces and trim only for swept volumes; variable scale/twist, end caps, miter-vs-round at polyline joints; validated on an L-path and a helix | 17 |
| [50-shell-offset-thicken.md](50-shell-offset-thicken.md) | Shell/hollow/thicken/whole-body offset: the inner shell as a maximal multi-face tweak (offset each face inward, re-intersect adjacent offsets reusing the file-03 extend-and-reintersect, the inner shell a combinatorial copy with convexity flipped, bridge pierced faces with rim walls, assemble via file 47); self-intersection collapse via the file-41 r_max = local feature size predictor with r := t (face-vanish collapse-merge vs face-cross escalate); per-face thickness and pierce-open; whole-body offset trimmed by generalized winding number (Jacobson, the 3D Chen-McMains); sheet thicken as two offsets plus a rim band; validated on a shelled box and a pierced cup | 17 |
| [51-sheet-bodies-ops.md](51-sheet-bodies-ops.md) | Sheet/open-body topology and ops in the PES: sheet-body validity (double-sided faces bordering the same void, free edges with radial cycle 1, open-shell Euler; the discriminator is an edge with radial cycle == 1); knit/sew (free-edge clustering plus Barequet-Sharir partial-overlap matching onto one tolerant edge) with close-detection that flips the bounded region void to solid and recomputes sense per file 46 (a flag flip, not a rebuild); surface extension as the one trim/untrim/heal primitive (analytic continuation, NURBS unclamp+extrapolate with reach/fold/weight guards); trim/split by shared imprint; validated on a six-sheet cube and a plane-trimmed sheet | 17 |
| [52-persistent-naming-keel.md](52-persistent-naming-keel.md) | Keel-specific persistent topological naming building on the file-07 survey: a Capoyleas-Chen-Hoffmann generative-history scheme (faces primary, edges by ordered bounding-face pair, Kripac-structured) carried as Keel's existing lineage edges and queried Cascaval-DSL style, because CCH's name = operation + operands + derivation kind already IS Keel's lineage map (promote-and-replay, not new machinery); the name structure and forward-replay re-resolution against a regenerated body (match derivation events, never coordinates); split/merge disambiguation by object-space anchor and canonical key; boolean name-carrying via the file-47 EdgeId radial groups (the two must ship together); validated on a box-chamfer-hole edit chain | 14 |
| [53-vertex-setback-blends.md](53-vertex-setback-blends.md) | The corner where two or three edge blends meet (item 51): the equal-radius perpendicular-or-not corner is an EXACT sphere octant / spherical polygon from a single inscribed sphere of radius r tangent to all incident faces (equal radius is the load-bearing condition, not perpendicularity), the ordinary corner (unequal radius, curved supports, n edges) reduces to a Varady-Rockwood setback-split convex-combination / Charrot-Gregory n-sided patch that is a closed-form evaluator Keel's certified evaluator-to-NURBS fit caches with no new surface type, only mixed-convexity / degenerate-setback / high-valence-G2 / overflow-into-corner need the full solver; the 2n-sided setback-split topology (n spring plus n profile curves alternating) and the full worked Euler-operator diagram for the equal-radius box corner (net +1 face, -1 vertex, 0 edges, Euler-Poincare checked), with the exact mass==mesh==reference oracle against the sphere | 12 |
| [54-partial-span-blends-runouts.md](54-partial-span-blends-runouts.md) | A blend over a segment of an edge terminating mid-edge (item 55): the analytic rung is that a linearly-tapering-to-zero radius between two planes is an EXACT right circular cone (apex at the vanishing point, half-angle arcsin(r0/L)), derived from the canal-surface characteristic circle (centre at d = r r', radius r sqrt(1 - r'^2)), with constant-radius span a cylinder and circular-spine span a torus, while eased / non-linear / curved-spine tapers degrade to a certified-NURBS canal fit; the mid-edge stop is a third extremity type beside dossier-44 cap-shrink/cap-grow (a cap_into_supports end realized by split_edge plus mev spurs plus mef, net +1 face, Euler-Poincare preserved), and imprint_spring_line generalizes by an additive endpoint-kind dispatch (OnBoundary / AtSharpStop / AtRunout) reusing split_face plus trim-strip dissolve unchanged | 21 |
| [55-blend-networks-mitring.md](55-blend-networks-mitring.md) | Chains and networks of edge blends with consistent junctions (item 56): the mitre (surface-surface-intersection trim) is the only junction treatment that adds ZERO faces versus the dossier-53 vertex patch's +1 face, so it is the cheap rung to try first; the decision procedure contracts smooth (tangent) chains into one continued surface, builds the unders blend-graph, orders largest-radius-first with any freeform-producing blend forced last, applies each ribbon with the file-40 engine, then resolves every non-smooth junction by the cheapest valid rung (mitre, then cliff/roll-over, then the dossier-53 setback-split patch) validating Euler-Poincare and mass==mesh after each step; the single highest-value missing piece is generalizing the spring-line imprint from planar to analytic curved supports (cylinder/cone/torus), and only freeform-on-freeform handoffs and cyclic mutually-dependent ribbons are irreducibly general | 12 |
| [56-blend-overflow-handling.md](56-blend-overflow-handling.md) | What to do when a blend is too big for its supports (item 57), consuming dossier 41's failure catalog and giving the HANDLING: the dominant CAD case is cliff overflow of a constant-radius cylinder fillet running off onto a perpendicular planar drop, handled by clipping the exact cylinder to the sharp cliff edge (an exact conic/ellipse trim), worked end-to-end with a 10-step Euler surgery (net -1V, -1E, +0F, Euler-Poincare preserved); the documented strict priority ladder is smooth, then cliff, then notch, then cap, with a four-predicate cheapest-complete classifier reusing attributes Keel already maintains; analytic cases stay closed-form (cliff-clip to an existing edge, coplanar roll-on, planar notch-bridge, planar cap), evaluator cases cache one pointwise evaluator (creeping cliff, smooth-merge transition patch, curved-neighbor roll-on), and the general tail (cascading curved roll-on with self-intersection, mixed-convexity, blend-blend mitre, swallowed-feature route-around) is declined until the exact rungs are solid; patent posture flagged (extended-rolling-ball US5774359 expired/free, notch sheet-sew US8935130 live, design-around via in-place Euler surgery) | 20 |
| [57-nonmanifold-general-booleans.md](57-nonmanifold-general-booleans.md) | Booleans on general / non-manifold bodies (item 29, the batch's deepest, gating whether the item is one milestone or three): item 29 is a CELL-COMPLEX SELECTION LAYER over the existing imprint/classify/glue pipeline plus three localized stitcher relaxations (radial-cycle arity, kept sub-3D cells, a dimension-pair-keyed keep predicate), NOT a stitcher rebuild, so it is ONE milestone (Rung 1, an exact two-region cube-fuse oracle) plus a follow-up ladder, contingent only on first retiring the dossier-47 polygon soup (build_result_solid cannot hold a radial-3 cycle or a kept wire/lamina); the literature is unanimous (Rossignac-O'Connor SGC subdivide/select/simplify, ACIS cellular-topology graph "keep vertices = keep cells", Pereira's dimension-and-region-keyed removal table) that the stitcher is shared across all dimension pairs and only the SELECTION varies, dossier 47's radial-splice already builds Weiler radial cycles of any length so 3-plus non-manifold edges are free, and on-sheet half-integer / undefined winding is resolved by the dossier-39 two-sided neighborhood test plus Spainhour-Weiss boundary-integral generalized winding number (watertightness-indifferent) | 26 |

## Headline findings across the collection

1. **The topology hierarchy is a solved consensus.** Parasolid, ACIS, OCCT, and STEP ISO 10303-42 describe essentially the same body-shell-face-loop-fin-edge-vertex model; fin = coedge = half-edge = oriented edge-use under four names. Adopt it without re-derivation (file 01).
2. **Go non-manifold from day one, via the partial entity structure.** PES gives radial-edge expressiveness at roughly half the storage; Parasolid makes regions first-class (the model to emulate), ACIS bolts cellular topology on (cautionary), OCCT refuses non-manifold boolean results (the gap) (file 02).
3. **Tweak is the foundation local operation.** Parasolid and ACIS both implement offset/move/taper/delete-heal/blend-removal as clients of change-face-surfaces-and-reintersect. OCCT lacks it entirely: a clear differentiator target for Keel (file 03).
4. **Delete-face cannot be decomposed** into simpler edits and must remain a dedicated primitive (Zou-Feng-Gao 2023 variational direct modeling) (file 03).
5. **Constraint solver above the kernel, profile geometry inside.** Siemens ships DCM beside Parasolid; one arrangement-plus-winding-number engine unifies region extraction, curved planar booleans, and offset cleanup inside the kernel (file 04).
6. **Watertight faceting is a by-construction property:** discretize each model edge once, have both adjacent faces consume the same polyline. All three industrial faceters converge on this (file 05).
7. **One multivariate spline solver unifies interrogation.** Silhouettes, draft isoclines, isophotes, curvature extrema, umbilics, and projection are all zero-set queries against the same subdivision solver already mandated by the intersection research (file 06, ties to nurbs/04).
8. **Persistent naming reduces to per-entity lineage.** Every scheme from Kripac (1995) to Cascaval's PLDI DSL (2023) is a variation on (operation id, operand ids, derivation kind: created/modified/split/merged/deleted). The kernel must emit these events from every operation, and must be deterministic, or no naming layer can be built above it. This must be settled before the topology layer freezes (file 07).
9. **Copy-on-write persistent topology buys three features at once:** undo, atomic failure rollback (a failed boolean must not corrupt the model), and cheap journaling snapshots. Parasolid's deltas and ACIS's bulletin boards are both approximations of what a Rust persistent data structure gives natively (file 07).
10. **Robustness claims need a corpus and a pass rate.** fTetWild's run-over-all-of-Thingi10K methodology plus the ABC dataset (1M B-reps) give Keel a concrete acceptance benchmark. Compiler FMA fusion can silently break orientation predicates; pin FP contraction and use Shewchuk predicates (file 08).
11. **Generational-index arenas are the Rust answer to entity references.** Parasolid tags, Fornjot's object store, and a generational arena are the same idea; it sidesteps borrow-checker pain on cyclic topology graphs and gives stable save/load references (file 08).
12. **Indirect predicates (Cherchi/Attene) give exact mesh booleans without bignum-everywhere.** EMBER's plane-based representation is architecturally sympathetic to B-rep (faces already carry carrier geometry). Parasolid Convergent Modeling's lesson is architectural: make mesh faces a first-class face type in the same topology (file 09).
13. **Lattices belong on the implicit side; winding number is the universal inside/outside oracle** across B-rep, mesh, and implicit, including leaky geometry (file 09, ties to nurbs/06).
14. **The grazing condition v.n = 0 unifies swept volumes and the medial axis** (envelope of moving solid; envelope of inscribed balls). One pruned MAT with a radius field feeds five services: midsurface, wall thickness, shelling validity, mesh sizing, defeaturing (file 10).
15. **Midsurfacing remains commercially unsolved** (2025 papers still propose new methods); treat it as a service with explicit failure reporting, not a guaranteed operation (file 10).

## Headline findings, second round (files 11-20)

16. **Exactness has a sharp affordability gradient by algebraic degree.** Circles and conics are genuinely exactness-affordable; quadric intersections are rational-when-singular and need one square root in the smooth case (QI); NURBS is effectively unbounded. CAD models are degeneracy-dense by design, exactly where floating-point filters fail. No shipped kernel delivers the consensus "exact topology + tolerant geometry" hybrid: Keel's opening (file 11).
17. **The witness method solves 3D constraint diagnosis.** There is no Laman analog in 3D, but for direct modeling the live B-rep IS a valid witness configuration, so a single Jacobian rank test gives correct over/under/well-constrained diagnosis. Langbein's beautification is an open-literature equivalent of Live Rules (file 12).
18. **Minimal-motion is THE direct-modeling solver requirement:** enforce detected regularities exactly while minimizing change from the current configuration (minimum-norm step, frozen null space). The same principle unifies solving, beautification, and symmetrization (file 12).
19. **All commercial healers converge on stitch, then simplify (analytic recognition), then geombuild (gap tightening).** Heal-vs-tolerate is a policy choice, and tolerant entities are contagious through booleans, arguing for a prefer-exact bias on boolean-bound bodies. Surface extension is the shared core of delete-face healing, tweak, and untrimming, and must be bounded and validated (file 13).
20. **Bitwise determinism is achievable but must be designed in:** FMA contraction can make orientation predicates non-antisymmetric (pin ffp-contract off on predicate code), system libm is the largest cross-platform hole (use correctly-rounded math), HashMap ordering and lossy decimal serialization are the residual Rust traps. Exact double round-tripping in files is load-bearing for persistent naming (file 14).
21. **Feature recognition's persistent hard problem is intersecting features**, and every era converges on the same answer: propose (hints or ML), then exactly verify in the kernel. ML benchmark accuracy is synthetically inflated; the kernel's job is to expose the attributed adjacency graph, exact surface classification, and a volumetric verifier (file 15).
22. **The assembly tier is universally a definition/occurrence DAG** with geometry stored once and transforms on references. The kernel obligations are stable edit-surviving IDs and keeping geometry in local frames (never bake far-from-origin world transforms into body coordinates) (file 16).
23. **Tolerance zones are offset geometry** (Requicha), and MMC/LMC virtual boundaries are offset solids, so the offset machinery is tolerance-critical. Datum establishment per Y14.5.1 is an existential optimization requiring Chebyshev/min-zone fitting, not just least squares. The dominant industry failure is semantic PMI loss on exchange, which again lands on persistent entity references (file 17).
24. **The patent landscape is navigable but has three active HIGH-risk zones:** Synchronous-style constraint inference during direct edits (Siemens, to ~2034), mixed mesh/B-rep in one body (convergent modeling, ~2035+), and U-splines (~2037). The original T-spline patent expired March 2024. The entire classical core (NURBS, Euler ops, booleans, classic blends, tessellation) is patent-safe. Recommended: MIT OR Apache-2.0 dual license for the patent grant (file 18).
25. **Parallel feature-history regeneration is an open gap:** industrial regeneration speed comes from caching and dependency pruning, not parallelism (a documented negative finding). The regeneration layer is a scheduler over deterministic, lineage-reporting, snapshot-able, fail-soft kernel operations; everything else stays above the kernel (file 19).
26. **Sheet metal reduces to an anchored bend graph that supports cycles**, with topology invariant between flat and folded states, thickness-paired faces as first-class citizens, exact cylindrical unwrap in the kernel, and bend compensation (K-factor, DIN 6935) as a pluggable rule layer, never kernel physics (file 20).

## Headline findings, third round (files 21-24)

27. **The motion data contract is uniform across engines:** MuJoCo, Drake, and Bullet all consume mass+inertia per body, joint axis+type, and collision geometry. The kernel's motion obligations are exactly those plus joint-axis extraction from face pairs and time-parameterized swept volumes. Effective mobility needs rank-based se(3) computation, not Gruebler joint counting (file 21).
28. **CAx-IF/LOTAR validation properties are the comparison keystone:** implementing volume/area/centroid/cloud-of-points natively gives Keel both the cheap stage of its own testing oracle and interoperability with every commercial model validator. All comparison methods (fingerprints, graph matching, learned encoders) consume the same kernel primitives: deterministic mass properties, controllable surface sampling, and an attributed adjacency graph (file 22).
29. **One orthogonal-distance fitting core serves four subsystems.** Datum simulation, healing simplify, reverse engineering, and canonical recovery are configurations (objective: L2/min-zone/MIC/MCC, robustness mode, constraint set) of a single fitting library. Exact optimality certificates exist (Voronoi for roundness, contact alternation for min-zone), so results can be certified, not just computed. Metrology mode forbids the outlier rejection that RE mode requires (file 23).
30. **The linear line complex is the universal axis recognizer:** one 6x6 eigenproblem on surface normals classifies cylinder vs revolution vs helix and recovers the axis, with pitch as the branch variable. Keel's recognition input is a noise-free spline (closed-form curvatures), removing the dominant error source in the scan-oriented literature. Substitution must be topology-coordinated (neighboring faces converted to the same analytic) per OCCT's design lesson (file 24).

## Parasolid coverage audit (file 25) and fourth-round findings (files 26-28)

A 144-item inventory of Parasolid PK functionality (file 25) was mapped against the full corpus. The result: the corpus already covers the large majority of Parasolid's surface (creation, all booleans including convergent, local operations, hollow/shell/offset, sweep/spin/loft, sheets, mesh/lattice, tessellation, HLR, all interrogation, tolerant modeling, attributes, sessions/rollback/XT, healing, simplification, canonical conversion, midsurface, Euler ops). Only three operation areas were uncovered or thin, and the fourth round closed them:

31. **N-sided surfaces require central-split, not a single patch.** For n != 4, or any irregular-valence node in a curve network, the four-corner twist structure plus Peters' vertex-enclosure parity constraint make a single tensor-product NURBS that is G1/G2 to all boundaries impossible; the n-gon must be split into n quad NURBS subpatches around a central point (what shipping kernels emit). The easy cases convert exactly: Coons and Gordon B-spline surfaces collapse to one NURBS, GB patches with polynomial boundaries to rational Bezier. The G1/G2 hole-fill primitive is a concrete 6-step procedure (file 26).
32. **Deform the control net, hold the parameter domain: trim curves and pcurves survive by construction.** This is the load-bearing B-rep deformation insight, absent from the graphics literature, and the reason to push NURBS control points rather than sample-and-refit. Barr's normal rule n' = det(J)(J^-1)^T n keeps normals, offsets, and draft correct under any differentiable deformation; Gain-Dodgson injectivity (det J > 0 in Bezier form) is the validity certificate. Green coordinates are the most CAD-relevant cage scheme (quasi-conformal). Emboss and wrap are composed operators (offset+trim+stitch; reverse-flatten then emboss), which argues for building flattening as a bidirectional map. Analytic faces promote to NURBS under deformation: it is the inverse of file-24 recovery (file 27).
33. **One spine + spring-curve + polymorphic-section engine serves fillets and chamfers alike.** The cross-section is the only variable: line for a chamfer, arc for a fillet, conic-by-rho for shaped, curvature-spline for G2. Chamfer is the linear member of the section family, not a separate code path, though it should keep an exact straight ruling. Parasolid and ACIS converge on the same overflow menu (smooth/cliff/notch/auto) and blend-graph sequencing, confirming the architecture is industry-standard. Hold-line blends are the engine run inverted (contact curve given, radius solved per section), so the section solver must be bidirectional. Vertex blends, overflow, and small-radius-vs-tolerance are the documented dominant failure modes (file 28).

## Dirty-geometry round (files 29-30) and XNURBS surfacing round (files 31-33)

The dirty-geometry round addresses what the user identifies as Parasolid's single strongest, most-differentiating capability: bad input still gets a usable answer where other kernels fail or decline.

34. **Correctness and robustness-on-dirty-data are orthogonal axes.** CGAL is exact but brittle (refuses imperfect input); OCCT is correct but all-or-nothing by default (its fuzzy boolean value is the only openly documented industrial knob, and a blunt one); Parasolid and ACIS manage tolerance automatically and return valid-but-salvaged bodies. The graceful path is a six-rung escalation cascade (filtered-exact, tolerant, fuzzy/widened-snap, controlled perturbation, virtual topology, mesh/SDF floor) under two hard rules: geometry is negotiable and reported, topology validity is non-negotiable. Qi-Shapiro epsilon-solidity gives a result a property to certify against ("valid epsilon-solid for reported epsilon"). The deepest finding: robustness is largely an organizational asset, the regression corpus (C3D's 500k+ models) is the real engine, and it is the one moat an open project can plausibly out-build (file 29).
35. **Virtual topology is the strongest tolerate-by-hiding technique:** treat a cluster of bad/tiny faces as one composite face without touching the geometry. Zero shape error, reversible, cannot create new self-intersections, so it belongs before any geometric repair on the mesh/query paths. Generalized winding numbers plus fTetWild form a robustness floor that mathematically cannot fail to return a valid manifold, with fidelity bounded by a caller-chosen envelope. Snapping must be a one-shot clustered union-find merge with per-merge local validation, never an unbounded relaxation loop (file 30).

The XNURBS round followed the Parasolid pattern: catalog (file 31) then research the one distinctive uncovered capability.

36. **XNURBS-class surfacing is variational, and it is the same engine the fitting research already converged on.** A single global energy minimization (membrane plus thin-plate fairness, Welch-Witkin 1992) over arbitrary mixed constraints, discretized to a control-point quadratic, reduces to (N^T W N + lambda K) P = N^T W Q subject to A P = b: data term, fairness penalty, and pure-variational solve are one engine at different weights, independently confirming the nurbs/03 fitting conclusion. The documented wavy-interior failure mode is explained directly: hard boundaries plus an under-weighted fairness term. Use transfinite (file 26) for clean fast N-sided fills, variational for messy/over-determined cases, with transfinite as the variational warm start (file 32).
37. **Matching a fixed neighbor surface linearizes to pinned control-point rows.** The bilinear G1/G2/G3 inter-patch conditions (DeRose, Liu-Hoschek) become linear when the neighbor is fixed: G1 pins one interior control-point row, G2 pins two, G3 pins three, which is exactly the Alias practical rule (degree 5 for G2, degree 7 for G3). The whole constraint machinery collapses to one KKT-solved constrained least squares with the file-32 fairness matrix, dirty input is absorbed as weighted soft constraints with slack (never infeasible, ties to files 29-30), conflicts are caught by rank-revealing factorization, and impossible corners (Peters vertex-enclosure from file 26, in algebraic form) are demoted to least squares (file 33).

## Engineering round (files 34-37): how it is made fast and proven correct

This round shifts axis from what the kernel computes to how it is built, tested, and run. It operationalizes the corpus-is-the-robustness-engine thesis (finding 34) that the rest of the research kept arriving at.

38. **Robustness is generated, not hoped for: the corpus must be built to break the kernel.** Public datasets (ABC, Thingi10K, Fusion 360 Gallery) are clean by construction and under-sample exact coincidence, which is measure-zero for random generators, so synthetic adversarial generators that snap geometry into tangency and coincidence are non-optional. Shrinking must target the construction recipe toward the degenerate witness, not perturb coordinates away from it. fTetWild's run-over-the-whole-dataset pass-rate (they report 98.7% on Thingi10K) is adopted as Keel's headline robustness metric, and every found failure becomes a permanent named golden case (file 34).
39. **The oracle problem is solved by invariants, not answers.** Without ground truth, correctness is checked by metamorphic relations: boolean algebra laws, round-trips, invariance under rigid transform and uniform scale, and the strongest of all, invariance under knot refinement and degree elevation (they perturb the internal representation while holding the modeled object fixed). Mass-property inclusion-exclusion and the topology validator are continuous oracles. The decisive layering: an epsilon-solidity certificate (Qi-Shapiro) becomes proof-grade only when its epsilon is bounded by a validated-numerics enclosure (Krawczyk, interval/affine) rather than estimated, and an exact oracle on the easy linear/low-degree cases breaks differential-testing ties when OCCT itself is the buggy reference. Formal verification realistically reaches predicates and small algorithms, not whole booleans (file 35).
40. **A kernel is a data-parallel numerical engine bolted to a serial graph-mutation engine, and copy-on-write is the keystone that lets both be true.** Intersection and classification read immutable COW snapshots lock-free; only the build/stitch phase serializes, the split OCCT (BOPAlgo) and Parasolid both independently reach. The determinism-versus-speed conflict has a rigorous resolution: binned/order-independent reduction (ReproBLAS) plus parallel-map-then-deterministic-merge (handle-sorted, no hash-order, no float reassociation) gives bitwise-identical results across thread counts while keeping work-stealing. The same no-reassociation rule blocks naive autovectorized reductions, so reductions need explicit fixed-order kernels. GPU is display-only: its float non-reproducibility is incompatible with the determinism promise on the exact-modeling path. Bezier extraction is the highest-leverage SIMD decision (branchy de Boor becomes a branch-free SoA GEMM) (file 36).
41. **The numerical substrate: sparse-direct by default, QR over normal equations, analytic derivatives over autodiff, portable interval widening.** Sparse direct Cholesky/QR (CHOLMOD lineage, now native in Rust via faer) beats tuned multigrid at kernel problem sizes and allows factor reuse, so it is the surfacing-solver default; the lambda-K fairness term doubles as a Tikhonov regularizer that conditions the system so plain Cholesky works. Spline structure gives analytic hodograph derivatives that beat autodiff where available. Rust rounding-mode control is awkward (LLVM assumes round-to-nearest, WASM lacks the instruction), so interval arithmetic should default to portable ULP-widening rather than FPU-mode flipping, which also helps determinism (file 37).

## STEP AP242 import (file 38): the one remaining shippable-path literature gap

42. **STEP import is a close-but-treacherous entity mapping, and the parser should be a focused pure-Rust build.** STEP's B-rep model maps almost one-to-one onto Keel's hierarchy (manifold_solid_brep to Body, closed_shell to Shell, advanced_face to Face, edge_loop to Loop, oriented_edge to Coedge, edge_curve to Edge, vertex_point to Vertex), but the high-risk pitfalls are: rational-NURBS weight pre-multiplication into homogeneous 4D (the silent-distortion trap), the orientation algebra (XOR of advanced_face.same_sense, face_bound.orientation, oriented_edge.orientation, and edge_curve.same_sense), seam edges on periodic surfaces appearing twice in one loop (one Edge, two Coedges), pole/degenerate faces via vertex_loop, non-normalized direction vectors, pcurve reconstruction when STEP gives only the 3D edge, and units plus the file's own uncertainty_measure_with_unit global tolerance (a STEP file declares its own accuracy, which seeds Keel's per-entity tolerance floor). Verdict: build a focused pure-Rust Part 21 tokenizer plus two-pass resolver over a hand-curated ~60-entity dictionary (borrow ruststep's Holder/tables pattern), do not FFI to OpenCASCADE or STEPcode, and treat OCC's reader plus its ShapeFix sequence as the conformance reference. Import is where Keel's tolerant/heal machinery (files 13, 29, 30) first meets foreign dirty data, and the CAx-IF validation properties (volume/area/centroid, file 22) are the round-trip check (file 38).

## Coincident and tangent face booleans (file 39): an M7b-critical-path request from the implementation team

43. **Coincident faces are preserved, not perturbed away, and the on-on classification reduces to two tables keyed on relative sense.** The Laidlaw-Trumbore-Hughes regularized-boolean rules, identical in outcome to Requicha-Voelcker set-membership and Mantyla nbr classification, say: a same-sense coincident overlap survives union and intersection (kept exactly once, its duplicate dropped), an opposite-sense overlap survives difference; everything else drops or reverses. The winding number is undefined exactly on a shared boundary, resolved by a two-sided epsilon-along-normal neighborhood test. Production kernels all share a gluing fast path (OCCT BOPAlgo GlueShift/GlueFull, Parasolid matched regions, ACIS prepare) that declares faces coincident so SSI is skipped, roughly 90% time saved on abutting input. The decisive philosophy split: Simulation of Simplicity perturbs coincidences away, but in CAD a flush mating face is design intent that must be preserved, so Keel keeps SoS only as a symbolic tie-break for which copy survives, never to erase coincidence. The genuinely hard residual is the NURBS overlap-region boundary, where canonical recovery (file 24) is a hard dependency and the tolerant path is the fallback (file 39).

## Blend/fillet surface generation (file 40): an M7b-critical-path request from the implementation team

45. **A constant-radius fillet is a canal surface along a spine, and the spine is the SSI of the two offset support surfaces, which makes the analytic cases fall out for free.** Offsetting each support face inward by r stays in-family (plane to plane, cylinder to coaxial cylinder, cone to coaxial cone, sphere to concentric sphere, torus to torus), so the spine is an analytic surface-surface intersection Keel already computes, and the blend is the envelope of spheres of radius r along it. The exact-analytic ladder: a straight spine gives a cylinder (plane-plane), a circular spine gives a torus (many plane-cylinder, plane-sphere, coaxial cases), natural-quadric pairs give a Dupin cyclide, and everything else is a NURBS canal approximated to tolerance by skinning radius-r section circles in rotation-minimizing frames. The cyclide verdict: do not add a primitive type, emit cyclides as exact rational biquadratic NURBS, keeping cylinder and torus as the fast exact path. G1 is automatic (the ball is tangent to both faces along the spring curves), and the spring curves are exact pcurves on both surfaces by construction. The trim-and-stitch is a local imprint (ties file 39, kernel/03), and all three production kernels use a declarative blend-recipe/blend-graph pattern; notably OCCT emits NURBS even for plane-plane, the gap Keel's exact ladder closes (file 40).

46. **The medial axis predicts every fillet failure: the maximum safe radius is the local feature size.** Overflow and radius-vs-feature, the dominant fillet failure modes, were elevated to a first-class treatment. The unifying result: r_max(t) = dist(spine(t), medial axis) = local feature size, and all eleven catalogued failure modes (self-intersection, off-face-end, roll-on, cross-feature, cliff, notch, thin-wall pinch, swallow-feature, blend-blend interference, vertex, over-large radius) reduce to the single medial inequality r > dist(spine, something), with 1/kappa_concave as a cheap local pre-filter, half-wall-thickness for pinch, and inter-ribbon distance for blend-blend. The design move is a pre-generation feasibility pass that samples the spine, queries local feature size, and emits a per-parameter radius ceiling and an overflow map BEFORE any surface is built, gating file 40's generator. The recovery policy is best-effort with full disclosure (predict, then roll-on or smooth, then radius-limit, then setback/cap/notch, then partial-plus-report, and only as last resort reject), always returning a valid body plus structured per-region diagnostics, the graceful-degradation doctrine (file 29) applied to filleting. Parasolid and ACIS are recovery-oriented with named overflow modes but keep the predictor internal; OCCT is rejection-oriented and fragile; Keel should expose the feasibility pass and offer named recovery (file 41).

## Non-English pass (files 42-43): does the English-only corpus leave gaps?

The whole corpus was built from English sources. This pass searched Russian and Chinese natively to test whether that cost anything. Honest verdict: the gap is small, the corpus already held the substance, but two genuinely new, directly-actionable items surfaced, both on the tolerant-modeling and architecture axes where C3D and the Chinese effort are strongest.

47. **C3D's tolerant-topology model is the cleanest tolerant-B-rep specification found in any language, and it is directly implementable.** From the Russian-only C3D Labs engineering blogs (the corpus had only ever cited C3D as the 500k-model robustness benchmark, never mined it for algorithmic detail): tolerance is bound to the TOPOLOGICAL entity, not the geometry. A tolerant vertex is a sphere of radius t, a tolerant edge is a tube of radius t, and the carried curve or surface stays exact inside that envelope. This sharpens Keel's existing per-entity-tolerance decision (D2) into a concrete geometric model, and pairs with two secondary C3D findings: per-operation boolean precision as a first-class API argument, and connectivity repair by tolerant-vertex insertion. The rest of the Russian material (topology structures, boolean engine, variational constraints, Soviet spline theory) the English corpus already had at equal or greater depth, and Golovanov's book has an authorized English translation, so it was never truly hidden (file 42).
48. **The Chinese journals hide no robustness silver bullet, but the domestic-kernel engineering and one intersection result are new.** A targeted search for a Chinese-only robust-boolean or self-intersection breakthrough returned nothing the corpus lacks; the Chinese practitioner literature converges on the same geometry-topology-consistency root cause (file 29) and the same OCCT all-or-nothing critique, which is corroboration, not novelty. What is genuinely new: the domestic-kernel picture, where Overdrive (ex-US VX) and CRUX (ex-Japan UEL) are acquired-and-domesticated and only AMCAX/Jiushao (USTC, 2017-2025) is a publicly-described from-scratch kernel, and it made the notable architectural inversion of building T-mesh splines as the foundational layer with NURBS B-rep added later. AMCAX independently choosing GMP plus MPFR plus TBB corroborates files 36-37, and ZWSOFT calling fillet/blend the kernel's "Mount Everest" independently confirms the three-dossier blend bet (28, 40, 41). The one adoptable algorithm nugget: a 2024 derivative-free curve-intersection iteration with order-2 convergence on tangential contact (2x-4x speedup exactly in the contact case file 39 flags as hardest), plus the PIA/LSPIA fitting lineage and its constrained CG-CLSPIA variant mapping onto files 32-33 (file 43).

## Concave-fillet topology surgery (file 44): an M7-tail-critical-path request from the implementation team

49. **A concave fillet grows the cap face and is the make-dominant dual of the convex kill-dominant dissolve; unify the two on a single convexity sign.** The convex fillet shrinks the cap face (split off the corner triangle, dissolve into the blend via kef then kev); the concave fillet does the opposite, the cap face grows, the reentrant corner vertex becomes interior, and the boundary extends out to the blend end-arc, so the convex split-off-and-dissolve is the wrong operation and yields a valid solid with the wrong volume. The grow-to-arc sequence (split_edge twice to land the spring curves, mev plus mef to build the exterior lune and the single blend face, kev to collapse the arc helper, kef twice to merge the lune, kev to absorb the reentrant corner) is each a GWB operator with a fixed delta, so Euler-Poincare is preserved at every step. The recommendation is one unified pipeline that forks only at the cap on the convexity sign sigma = sign((n1 x n2).t), Keel's equivalent of OCCT ConcaveSide: sigma > 0 cap-shrink-dissolve, sigma < 0 cap-grow-splice, with the spine/spring/offset signs already correct from file 40. Use in-place loop-regrow for the clean degree-3 plane-plane cap (exact, code-shared with convex), and fall back to bead-construct-and-sew for multi-face caps, into-face/into-blend ends, and mixed-convexity ends, sewing the tangent spring seam by file 39's two-sided method as a tolerant edge (file 42's tube-of-radius-t model). All three production kernels confirm the pattern: one pipeline with a convexity classifier that extends rather than trims the support faces at the concave extremity (file 44).

## Genus-1 solid-of-revolution Euler sequence (file 45): an M-tail-critical-path request from the implementation team

50. **A tube/annular solid of revolution is built by closing the meridian cycle then punching exactly one handle, and the degeneracy branch is a pre-punch guard, not a post-hoc check.** Generalizing the single-face torus recipe to n distinct faces: mvfs seed, then per meridian segment a mev (drop the next latitude vertex) and mef (close the band into a face), then a final mef that closes the cycle by reusing the seed vertex v0 (making the two open ends coincident), then a single kfmrh that punches the handle (genus 0 to 1) and nowhere else. For the hollow cylinder this lands exactly on V=4, E=8 (4 latitude circles plus 4 meridian seams), F=4, R=0, S=1, G=1, so V-E+F-R = 0 = 2(S-G). Holed annular caps use post-hoc kemr-the-seam (Keel's current revolve approach is correct; there is no genuine mekr-during alternative because mekr is the ring-consuming inverse). The seam is per-band (one seam edge per lateral face, used both directions in one loop, the OCCT cylinder model; a single shared seam would be non-manifold) and is orthogonal to the handle: the seam carries u-periodicity, the kfmrh carries meridian closure, and the punch leaves every seam edge intact. Production kernels (Parasolid, ACIS, OCCT, C3D) revolve or sweep a closed profile into periodic surfaces and get genus-1 emergently from the closed periodic meridian, never an explicit kfmrh, so Keel's Euler-only path must make the punch explicit. Critical guard: decide the handle BEFORE punching, if any vertex is a pole or the inner radius collapses to zero (within tolerance), genus stays 0 and the kfmrh is skipped (build collapsed bands as revolve-style apexes); Euler-Poincare alone will not catch a wrongly-punched handle, so it must be a precondition (file 45).

## Face-orientation sense convention (file 46): the architectural fix for a load-bearing inconsistency

51. **Face orientation is over-encoded; pick one source of truth and make sense a derived cache.** A face's orientation is described redundantly by four quantities (surface natural normal, sense bool, loop/fin orientation, region material side) that must satisfy one invariant: n_out = sense x natural_normal, n_out points away from material, and the outer loop runs CCW about n_out. Keel's two producers split the convention (Euler faces treat fin orientation as authoritative, boolean faces treat the natural normal as authoritative), so making mass_properties sense-aware fixes the Euler-built genus-1 tube but regresses the booleans. Production kernels (OCCT TopoDS Orientation, ACIS FACE sense, Parasolid) all make the per-face sense-relative-to-surface bit authoritative; STEP carries the flags redundantly and demands consistency. The recommendation for Keel is the opposite of the production default and is justified by the kernel's own degeneracies: fin/material-primary, with sense as a derived cache (sense = sign(dot(material_outward, natural))), because natural normals flip under reparameterization and vanish at poles and seams (exactly the file-45 cases), and because this leaves the harder Euler path unchanged and aligns the analytic path with the already-correct material-based mesh path. The fix is structural: one canonicalize_face_orientation routine as the sole writer of sense (both producers call it; the boolean path stops setting sense directly and instead orients its fins from the outward normal it already computes, which removes its ability to set sense wrong), one face.outward_normal() helper as the sole consumer (mass_properties, mesh, classification all call it), and a debug validator invariant (sign(dot(n_out, natural)) == sense AND dot(n_out, loop_normal) > 0 AND dot(n_out, region_void_dir) > 0, skipping the region clause for lamina faces). One shared outward-normal helper collapses the genus-1 mass_properties bug and the tilted-cut stitch-sense bug into a single fix, because they are the same divergence-integrand-sign error read through the two conventions (file 46).

## Boolean result assembly (file 47): the highest-leverage unblock

52. **A boolean fragment is dropped when assembly re-derives edge sharing geometrically; the fix is to carry edge identity from the imprint and pair coedges by it.** The asymmetric-chamfer face-drop is an assembly bug, not classification: the tilted fragment is correctly selected but build_result_solid (a polygon-soup stitcher) dedups vertices by coordinate, which collapses the thin/oblique feature, cannot represent a non-manifold radial cycle, and rejects the oblique face against the axis-aligned majority. The drop-prevention invariant: every coedge of every kept face must have exactly one radial partner (or a complete consistent radial cycle in the non-manifold case) before the shell is declared closed, and an unmatched coedge is a hard error to surface, never a face to silently drop (add a kept-in == faces-out count assertion). The robust assembly is identity-preserving: the imprint/seam stage already created the shared edges, so every fragment coedge carries a stable EdgeId, and assembly groups coedges by EdgeId and radial-splices, never re-welding by coordinate. Reference kernels do not build from polygon soup; they imprint-and-stitch on shared topology. Verdict: retire build_result_solid and generalize the existing Euler-glue path (stitch_by_import) to take planar fragments with their imprint identity, making it the single assembly path. Tolerance: do not weld by coordinate at all, reserve tolerance for imprint-flagged near-coincidence; where a weld is unavoidable use weld_eps_local = k * min(local edge length) and forbid welds that collapse a real feature (a single global epsilon is an unresolvable contradiction between feature preservation and coincidence welding). From mesh-arrangement booleans, the transferable principles are completeness-then-labeling (Zhou), identity-not-coordinate (Cherchi), local arrangement (EMBER), and above all Sugihara-Iri topology-decided-combinatorially-geometry-conforms; exact integer/plane predicates do not transfer to curved analytic B-rep, which is exactly why identity-from-imprint matters more for Keel. The kept reversed flag must propagate to the fin orientation per file 46 so the cavity wall bounds the solid on the correct side (file 47).

## Construction and architecture round (files 48-52): the queued frontier requests

53. **Smooth NURBS skinning never union-merges knots by default.** Degree-elevate sections to a common p, determine a small common u-knot vector (Park-Lee) and refit each section onto it by constrained energy minimization, reparameterize by feature correspondence to kill twist, then solve the v-direction by a per-column banded interpolation. G1/G2 ends and guide rails become pinned v-control-rows feeding the file-33 constrained-LS (rails turn the loft into a Gordon surface); ruled is the q=1 case; periodic sections need seam-origin alignment and a 1D twist-minimizing search; post-loft Lyche-Morken knot removal recovers up to ~99% of control points. Keep the procedural sections as the recoverable truth (file 48).
54. **General sweep uses double-reflection RMF, and its self-intersection ceiling is the same r_max as fillets.** Double-reflection RMF transport (Wang et al., O(h^4), exact on circular arcs) beats Frenet, which flips at inflections and is undefined on the straight legs of an L-path. Represent the sweep procedurally (spine + profile + frame) and bake to NURBS by tolerance-controlled skinning with a chord-height sampling rule. The swept surface folds iff the profile radius reaches the path curvature radius (r*kappa >= 1), the same local-feature-size ceiling as blend overflow (41) and the grazing condition (10): decline for surfaces, trim to the singular set only for swept volumes. Polyline joints use a bisector-plane reflection, miter (sharp) or round (G1 arc) (file 49).
55. **Shell is a maximal multi-face tweak: the inner shell is a combinatorial copy of the outer with convexity flipped.** Offset every face inward by t and re-intersect adjacent offsets reusing the file-03 extend-and-reintersect, so only geometry is recomputed (face/edge/vertex one-for-one), then bridge pierced faces with rim walls and assemble via file 47. Self-intersection reuses the file-41 r_max = local-feature-size predictor with r := t (the user-facing "thickness < smallest concave radius" rule): a vanishing face collapse-merges, crossing faces escalate or locally reduce, always returning a valid body with disclosure. Whole-body offset builds the raw self-intersecting offset then extracts the valid region by generalized winding number (Jacobson, the 3D Chen-McMains). Sheet thicken is two offsets plus a rim band (file 50).
56. **A sheet body is just a PES region configuration, and knitting that closes a void is a flag flip, not a rebuild.** The PES represents sheets with no new topology: double-sided faces bordering the same ambient void, free edges with radial cycle 1, open-shell Euler; the single discriminator from a solid is "exists an edge with radial cycle == 1." Knit/sew clusters free edges and matches them (Barequet-Sharir partial-overlap) onto one tolerant edge; when the free-edge set empties and the genus closes, the bounded region flips void to solid and faces go double- to single-sided with a file-46 sense recompute (the regression-risk spot). Surface extension is the one shared primitive behind trim, untrim, and delete-face-heal (file 51).
57. **Persistent naming is promote-and-replay over Keel's existing lineage, and it must ship with the file-47 assembly.** The right scheme is Capoyleas-Chen-Hoffmann generative-history (faces primary, edges by ordered bounding-face pair, Kripac-structured), because CCH's name = operation + operands + derivation kind already IS Keel's lineage map. A name is a lineage path of op:kind:selector steps rooted at host-durable feature anchors plus one object-space anchor; re-resolution forward-replays each step as a query over the regenerated body's live lineage, matching derivation events and never coordinates. Splits resolve by ordinal then nearest-anchor then canonical key; merges suggest the ancestor or orphan (never a confident wrong bind). Boolean name-carrying rides the same file-47 EdgeId radial groups that prove shell closure, so persistent naming and the identity-preserving assembly must ship together (file 52).

## Last parity-engine round (files 53-57): the final tractable items on the Parasolid map

This round answers the five remaining tractable Parasolid-map items (51, 55, 56, 57, 29) that each needed a dedicated engine, the move from 132/144 toward the ceiling. The unifying question asked of every dossier was the kernel's recurring one: which configurations have an exact analytic rung, which reduce to a certified evaluator fit Keel already caches, and which genuinely require the full general machinery. The answer held across all five: the exact or evaluator rung covers the common case, and the irreducibly-general tail is small and can be declined until the rungs are solid.

58. **The equal-radius corner is an exact sphere octant; every other corner is a setback-split evaluator patch Keel already caches.** Where two or three edge blends meet (item 51), the equal-radius corner is closed form: a single inscribed sphere of radius r tangent to all incident faces gives a sphere octant for perpendicular planes and a spherical polygon otherwise, with EQUAL RADIUS (not perpendicularity) the load-bearing condition. The ordinary corner (unequal radius, curved supports, n incident edges) reduces to the Varady-Rockwood setback-split: retract each edge blend by a setback distance and fill the resulting 2n-sided hole (n spring curves alternating with n profile curves) with a Charrot-Gregory convex-combination patch, which is a closed-form black-box evaluator that Keel's certified evaluator-to-NURBS fit pipeline caches with no new surface type in the store. Only mixed-convexity, degenerate-setback, high-valence-G2, and overflow-into-corner corners need the full solver, and even those reuse the setback-split skeleton. The deliverable is the full worked Euler-operator topology diagram for the equal-radius box corner: the three spring ends and three cap arcs meet at a single new spherical face, netting +1 face and -1 vertex (0 edges), with Euler-Poincare checked, and the first milestone is the exact mass==mesh==reference oracle against the sphere |x - (r,r,r)| = r (file 53).
59. **A blend that tapers to zero radius between two planes is an EXACT right circular cone, and a mid-edge stop is a third extremity type.** For a range-controlled / partial-span blend (item 55), the central analytic result is that a constant-radius blend whose radius ramps linearly to zero over a runout length L between two planes is an exact right circular cone (apex at the vanishing point, half-angle arcsin(r0/L)), derived rigorously from the canal-surface envelope (the characteristic circle sits at distance d = r r-prime along the spine with radius r sqrt(1 - r-prime squared), giving a constant half-angle for a linear ramp), a developable in its sharpest form with no spline needed; the constant-radius span is a cylinder and a circular-spine span is a torus, while easing the radius law or curving the spine degrades the runout to a non-developable canal stored as a certified NURBS fit. The mid-edge stop (the blend's end boundary lies inside the two support faces, not on a cap) is a third blend-extremity type beside dossier-44's cap-shrink and cap-grow: a cap_into_supports end, realized by a worked GWB Euler sequence (split_edge on the sharp edge for the junction vertex, mev spurs for the interior spring-line landings, mef for the cross curve, net +1 face, Euler-Poincare preserved), and the existing cap-to-cap imprint_spring_line generalizes by an additive endpoint-kind dispatch (OnBoundary / AtSharpStop / AtRunout) that reuses split_face plus the trim-strip dissolve unchanged (file 54).
60. **The mitre is the zero-face junction, so a blend network is a cheapest-rung decision procedure over Keel's existing operations.** For chains and networks of edge blends (item 56), the key economic fact is that a mitred junction is a surface-surface-intersection trim that adds ZERO faces, versus the dossier-53 vertex patch which adds one, so the mitre is the rung to attempt first. The decision procedure mapping a marked edge set to executable operations: contract smooth (tangent) chains into one continued surface, build the unders blend-graph (the cross-edge support-chain data structure from the blend-removal literature), order the ribbons largest-radius-first with any freeform-producing blend forced last, apply each ribbon with the file-40 single-edge engine, then resolve every non-smooth junction by the cheapest valid rung (mitre via SSI-trim, then cliff/roll-over, then the dossier-53 setback-split patch), validating Euler-Poincare and mass==mesh after each step. The benign majority of networks reduces to repeated single-edge surgery plus the dossier-53 corner; the single highest-value missing piece is generalizing the spring-line imprint from planar to analytic curved supports (cylinder/cone/torus), which unblocks sequential blends whose supports an earlier blend left curved, and only freeform-on-freeform handoffs and cyclic mutually-dependent ribbons are irreducibly general (file 55).
61. **Blend overflow handling is dominated by one exact case: clip the cylinder to the cliff edge.** Consuming dossier 41's failure catalog and supplying the handling algorithms (item 57), the Pareto-dominant case in real CAD is cliff overflow of a constant-radius cylinder fillet that runs off its support onto a perpendicular planar drop, handled exactly by clipping the analytic cylinder to the sharp cliff edge (an exact conic/ellipse trim), worked end-to-end with a 10-step Euler-operator surgery (net -1 vertex, -1 edge, +0 faces, Euler-Poincare preserved per step) and a closed-form mass==mesh==reference oracle plus a radius-sweep differential test. The documented strict priority ladder (T-FLEX, ACIS) is smooth, then cliff, then notch, then cap, decided by a four-predicate cheapest-complete classifier reusing attributes Keel already maintains. Analytic handling covers cliff-clip-to-an-existing-edge, coplanar roll-on, planar notch-bridge, and planar cap; an evaluator fit caches the creeping cliff (ball-on-edge canal), the smooth-merge transition patch, and curved-neighbor roll-on; the general tail (cascading curved roll-on with self-intersection, mixed-convexity ends, blend-blend mitre, swallowed-feature route-around) is declined until the exact rungs are solid. Patent posture is explicit: the extended-rolling-ball US 5,774,359 is expired and free for roll-on, while the notch sheet-sew US 8,935,130 is live and is designed around by Keel's in-place Euler surgery (file 56).
62. **Item 29 is a cell-complex SELECTION layer over the existing pipeline, not a stitcher rebuild, so it is one milestone.** The batch's deepest dossier answers the gating question for general / non-manifold booleans (item 29): can it be a cell-complex layer over the current pipeline, or does it force a stitcher rebuild? The defensible answer is the layer. Classify every fragment against both operands and keep cells per a SELECTION TABLE generalized to dimension pairs (solid/solid, solid/sheet, sheet/sheet, wire/solid, and so on, for union/intersection/difference, regularized and non-regularized), which is exactly what the literature mandates unanimously: Rossignac-O'Connor SGC (subdivide, select, simplify), the ACIS cellular-topology graph ("keep vertices = keep cells"), and Pereira's dimension-and-region-keyed removal table all share ONE stitcher across all dimension pairs and vary only the selection. Dossier 47's identity-preserving radial-splice already builds Weiler radial cycles of any length, so 3-plus non-manifold edges come for free; the only hard dependency is that the soup build_result_solid cannot hold a radial-3 cycle or a kept wire/lamina, so the dossier-47 retirement of the soup is the precondition. On-sheet points where the winding number is undefined or half-integer are resolved by the dossier-39 two-sided neighborhood test together with the Spainhour-Weiss boundary-integral generalized winding number (watertightness-indifferent for trimmed NURBS). Net: item 29 is one milestone (Rung 1, an exact two-region cube-fuse oracle) plus a follow-up ladder to sheet-sheet, wire imprinting, and full cellular results, with arbitrary mixed solid-plus-sheet-plus-wire bodies and non-regularized simplification the only genuinely-general tail (file 57).

## Decisions this research forces before code freezes

- Manifold-only vs non-manifold topology core (file 02 says: non-manifold, PES-class)
- Lineage-event emission contract on every operation (file 07: day-one mandate)
- Determinism guarantees (stable iteration order, deterministic IDs) as API promises (files 07, 08)
- Mesh faces as first-class topology citizens vs separate representation (file 09)
- The in-kernel vs above-kernel line for constraint solving (file 04: above) and tessellation (file 05: inside)
- The predicate strategy per geometry tier: exact for linear/conic, exact-when-cheap for quadrics, certified-numeric topology plus tolerant geometry for splines (file 11)
- The determinism contract: pinned FP contraction on predicates, correctly-rounded math, stable container ordering, exact double round-tripping in serialization (file 14)
- License choice with a patent grant (MIT OR Apache-2.0 dual) and avoidance of the three active patent zones: auto-inferred constraints during direct edits, mixed mesh/B-rep single bodies, U-spline-class refinement (file 18)

## Verification caveats

As with the NURBS collection, several paywalled, scanned, or blocked sources (ACM/ScienceDirect PDFs, Siemens/Spatial documentation mirrors, old theses, one patent) could not be fetched in full text. Agents reconstructed mechanism detail from accessible mirrors, abstracts, secondary expositions, and established domain knowledge; citations (author, title, venue, year) were verified by search. Spot-check primary sources before relying on exact constants, page numbers, or operator-level details, in particular the Parasolid/ACIS session mechanics (file 07), the Baumgart and Requicha originals (file 01), and the blend-removal patent material (file 03).
